export interface ItemModel {
    id: number
    name: string,
    price: number,
    quantity: number,
}

export interface ItemModelInCar {
    id: number
    name: string,
    price: number,
    quantity: number,
    subtotal: number,

}